local FruitFarmRunning = false
if FruitFarmRunning then return end
FruitFarmRunning = true

getgenv().WebhookUrl = getgenv().WebhookUrl or ""
getgenv().autoteam = getgenv().autoteam or "Pirates"

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local GuiService = game:GetService("GuiService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Workspace = game:GetService("Workspace")

local plr = Players.LocalPlayer
local Remote = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("CommF_")

local CONFIG = {
    MaxBlacklist = 30,
    BlacklistFile = "fruit_farm_blacklist.txt",
    FruitList = {
        "Dragon-Dragon", "Kitsune-Kitsune", "Leopard-Leopard", "Spirit-Spirit",
        "Control-Control", "Venom-Venom", "Shadow-Shadow", "Dough-Dough",
        "T-Rex-T-Rex", "Mammoth-Mammoth", "Gravity-Gravity", "Blizzard-Blizzard",
        "Pain-Pain", "Rumble-Rumble", "Portal-Portal", "Phoenix-Phoenix",
        "Sound-Sound", "Spider-Spider", "Love-Love", "Buddha-Buddha",
        "Quake-Quake", "Magma-Magma", "Ghost-Ghost", "Barrier-Barrier",
        "Rubber-Rubber", "Light-Light", "Diamond-Diamond", "Dark-Dark",
        "Sand-Sand", "Ice-Ice", "Falcon-Falcon", "Flame-Flame",
        "Spike-Spike", "Smoke-Smoke", "Bomb-Bomb", "Spring-Spring",
        "Chop-Chop", "Spin-Spin", "Rocket-Rocket", "Eagle-Eagle",
        "Creation-Creation",
        "Lightning-Lightning",
        "Gas-Gas",
        "Tiger-Tiger",
        "Yeti-Yeti",
    }
}

local FruitInternal = {
    ["Rocket Fruit"] = "Rocket-Rocket",
    ["Spin Fruit"] = "Spin-Spin",
    ["Blade Fruit"] = "Blade-Blade",
    ["Spring Fruit"] = "Spring-Spring",
    ["Bomb Fruit"] = "Bomb-Bomb",
    ["Smoke Fruit"] = "Smoke-Smoke",
    ["Spike Fruit"] = "Spike-Spike",
    ["Flame Fruit"] = "Flame-Flame",
    ["Ice Fruit"] = "Ice-Ice",
    ["Sand Fruit"] = "Sand-Sand",
    ["Dark Fruit"] = "Dark-Dark",
    ["Eagle Fruit"] = "Eagle-Eagle",
    ["Diamond Fruit"] = "Diamond-Diamond",
    ["Light Fruit"] = "Light-Light",
    ["Rubber Fruit"] = "Rubber-Rubber",
    ["Ghost Fruit"] = "Ghost-Ghost",
    ["Magma Fruit"] = "Magma-Magma",
    ["Quake Fruit"] = "Quake-Quake",
    ["Buddha Fruit"] = "Buddha-Buddha",
    ["Love Fruit"] = "Love-Love",
    ["Creation Fruit"] = "Creation-Creation",
    ["Spider Fruit"] = "Spider-Spider",
    ["Sound Fruit"] = "Sound-Sound",
    ["Phoenix Fruit"] = "Phoenix-Phoenix",
    ["Portal Fruit"] = "Portal-Portal",
    ["Lightning Fruit"] = "Lightning-Lightning",
    ["Pain Fruit"] = "Pain-Pain",
    ["Blizzard Fruit"] = "Blizzard-Blizzard",
    ["Gravity Fruit"] = "Gravity-Gravity",
    ["Mammoth Fruit"] = "Mammoth-Mammoth",
    ["T-Rex Fruit"] = "T-Rex-T-Rex",
    ["Dough Fruit"] = "Dough-Dough",
    ["Shadow Fruit"] = "Shadow-Shadow",
    ["Venom Fruit"] = "Venom-Venom",
    ["Gas Fruit"] = "Gas-Gas",
    ["Spirit Fruit"] = "Spirit-Spirit",
    ["Tiger Fruit"] = "Tiger-Tiger",
    ["Yeti Fruit"] = "Yeti-Yeti",
    ["Kitsune Fruit"] = "Kitsune-Kitsune",
    ["Control Fruit"] = "Control-Control",
    ["Dragon Fruit"] = "Dragon-Dragon"
}

local blacklist = {}
local fruitLog = {}
local isTeleporting = false
local pendingHopServer = nil
local statusLabel = nil
local collectedCount = 0

local function SaveBlacklist()
    pcall(function()
        if writefile then
            local data = table.concat(blacklist, "\n")
            writefile(CONFIG.BlacklistFile, data)
        end
    end)
end

local function LoadBlacklist()
    pcall(function()
        if not isfile or not readfile then return end
        if not isfile(CONFIG.BlacklistFile) then blacklist = {} return end
        local content = readfile(CONFIG.BlacklistFile)
        if not content or content == "" then blacklist = {} return end
        blacklist = {}
        for line in string.gmatch(content, "[^\r\n]+") do
            if line ~= "" and not table.find(blacklist, line) then
                table.insert(blacklist, line)
            end
        end
        if #blacklist >= CONFIG.MaxBlacklist then
            blacklist = {}
            SaveBlacklist()
        end
    end)
end

local function IsBlacklisted(serverId)
    return table.find(blacklist, serverId) ~= nil
end

local function AddToBlacklist(serverId)
    if not serverId or serverId == "" then return end
    if IsBlacklisted(serverId) then return end
    if #blacklist >= CONFIG.MaxBlacklist then
        blacklist = {}
        SaveBlacklist()
    end
    table.insert(blacklist, serverId)
    SaveBlacklist()
end

local function SelectTeam()
    -- Bọc toàn bộ trong pcall để bắt mọi lỗi
    local success, err = pcall(function()
        if not game:IsLoaded() then
            game.Loaded:Wait()
        end
        task.wait(3)
        
        local realteam = getgenv().autoteam or "Pirates"
        local pg = plr:FindFirstChild("PlayerGui")
        
        -- Nếu không có PlayerGui thì thoát
        if not pg then
            return
        end
        
        local mainmal = pg:FindFirstChild("Main (minimal)")
        
        -- Nếu không có Main (minimal) thì thoát, không báo lỗi
        if not mainmal then
            return
        end
        
        local chooseTeam = mainmal:FindFirstChild("ChooseTeam")
        if not chooseTeam then
            return
        end
        
        local container = chooseTeam:FindFirstChild("Container")
        if not container then
            return
        end
        
        local teamContainer = container:FindFirstChild(realteam)
        if not teamContainer then
            return
        end
        
        local frame = teamContainer:FindFirstChild("Frame")
        if not frame then
            return
        end
        
        local chooseteambtn = frame:FindFirstChild("TextButton")
        if not chooseteambtn then
            return
        end
        
        -- Click button
        local inset = GuiService:GetGuiInset()
        local pos = chooseteambtn.AbsolutePosition
        local size = chooseteambtn.AbsoluteSize
        local x = pos.X + size.X / 2
        local y = pos.Y + size.Y / 2 + inset.Y
        
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 0)
        task.wait()
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 0)
        
        task.wait(2.5)
    end)
    
    -- Nếu có lỗi, chỉ in ra console để debug, không ảnh hưởng script
    if not success then
        warn("⚠️ SelectTeam skipped: " .. tostring(err))
    end
end

local function isInCharacter(obj)
    local parent = obj.Parent
    while parent do
        if parent:FindFirstChild("Humanoid") then
            return true
        end
        parent = parent.Parent
    end
    return false
end

local function BringFruit(tool)
    if not tool or not tool:IsA("Tool") then return false end
    if not string.find(tool.Name, "Fruit") then return false end
    if isInCharacter(tool) then return false end
    
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    local handle = tool:FindFirstChild("Handle")
    
    if hrp and handle then
        handle.CFrame = hrp.CFrame + Vector3.new(0, 0, 0)
        return true
    end
    return false
end

local function ScanFruits()
    local found = {}

    pcall(function()
        for _, v in ipairs(Workspace:GetDescendants()) do
            if v:IsA("Tool") and string.find(v.Name, "Fruit") then

                local isValid = false

                for _, fruitName in ipairs(CONFIG.FruitList) do
                    local shortName = string.gsub(fruitName, "-.*", "")

                    if string.find(v.Name, fruitName)
                        or string.find(v.Name, shortName)
                    then
                        isValid = true
                        break
                    end
                end

                if isValid then
                    print("[FruitFarm] Found fruit:", v.Name)

                    if BringFruit(v) then
                        table.insert(found, v.Name)
                    end

                    task.wait(0.3)
                end
            end
        end
    end)

    return found
end

local function storeAllFruits()
    local stored = {}

    local function tryStore(tool)
        if not tool or not tool:IsA("Tool") then
            return
        end

        if not string.find(tool.Name, "Fruit") then
            return
        end

        local internal = FruitInternal[tool.Name]

        if not internal then
            warn("[FruitFarm] Missing internal name:", tool.Name)
            return
        end

        local success, result = pcall(function()
            return Remote:InvokeServer("StoreFruit", internal, tool)
        end)

        if success and result then
            table.insert(stored, tool.Name)

            print(
                "[FruitFarm] Stored:",
                tool.Name
            )
        else
            warn(
                "[FruitFarm] Store failed:",
                tool.Name,
                tostring(result)
            )
        end
    end

    for _, tool in ipairs(plr.Backpack:GetChildren()) do
        tryStore(tool)
    end

    if plr.Character then
        for _, tool in ipairs(plr.Character:GetChildren()) do
            tryStore(tool)
        end
    end

    return stored
end

local function SendWebhook(fruitName)
    if _G.WebhookUrl and _G.WebhookUrl ~= "" then
        pcall(function()
            local embed = {
                ["embeds"] = {{
                    ["title"] = "🍎 Fruit Collected",
                    ["color"] = 0xFF0000,
                    ["fields"] = {
                        {["name"] = "Fruit", ["value"] = "```" .. fruitName .. "```", ["inline"] = true},
                        {["name"] = "Player", ["value"] = "```" .. plr.Name .. "```", ["inline"] = true},
                        {["name"] = "Server", ["value"] = "```" .. game.JobId .. "```", ["inline"] = false}
                    },
                    ["footer"] = {["text"] = "Foxname ss2 Fruit Farm - by Malority"},
                    ["timestamp"] = os.date("!%Y-%m-%dT%TZ")
                }}
            }
            HttpService:PostAsync(getgenv().WebhookUrl, HttpService:JSONEncode(embed), Enum.HttpContentType.ApplicationJson)
        end)
    end
end

local function HopToRandomServer(retryCount)
    retryCount = retryCount or 0
    if retryCount > 5 then
        UpdateStatus("❌ Hop failed after 5 retries", Color3.fromRGB(255,80,80))
        return false
    end

    if isTeleporting then
        UpdateStatus("⏳ Already hopping...", Color3.fromRGB(255,200,0))
        return false
    end

    -- Blacklist server hiện tại trước khi rời
    if game.JobId and game.JobId ~= "" then
        AddToBlacklist(game.JobId)
    end

    isTeleporting = true
    UpdateStatus("🚀 Hopping attempt " .. (retryCount+1) .. "/5", Color3.fromRGB(255,100,100))

    -- Teleport đến place instance KHÔNG chỉ định server -> Roblox tự chọn server random (không vào full)
    local success, err = pcall(function()
        TeleportService:Teleport(game.PlaceId, plr)
    end)

    if not success then
        warn("[Hop] Teleport call failed: " .. tostring(err))
        -- Blacklist server fail
        if game.JobId and game.JobId ~= "" then
            AddToBlacklist(game.JobId)
        end
        isTeleporting = false
        -- Retry sau delay ngẫu nhiên
        local delay = math.random(3, 8)
        UpdateStatus("⏳ Retry in " .. delay .. "s", Color3.fromRGB(255,150,0))
        task.wait(delay)
        return HopToRandomServer(retryCount + 1)
    end

    -- Nếu teleport thành công, script sẽ bị ngắt do tải lại game
    return true
end

-- Bắt sự kiện teleport fail (nếu có)
TeleportService.TeleportInitFailed:Connect(function(teleportPlayer, teleportResult, errorMessage)
    if teleportPlayer ~= plr then return end
    warn("[Hop] TeleportInitFailed: " .. tostring(teleportResult) .. " - " .. tostring(errorMessage))
    if game.JobId and game.JobId ~= "" then
        AddToBlacklist(game.JobId)
    end
    isTeleporting = false
    -- Tự retry sau 3s
    task.wait(3)
    HopToRandomServer(0)
end)

TeleportService.TeleportInitFailed:Connect(function(
    teleportPlayer,
    teleportResult,
    errorMessage
)
    if teleportPlayer ~= plr then
        return
    end

    if not pendingHopServer then
        return
    end

    local failedServer = pendingHopServer

    warn(
        "[ServerHop] Teleport failed:",
        failedServer,
        teleportResult,
        errorMessage
    )

    -- Server full / chết / teleport lỗi
    AddToBlacklist(failedServer)

    pendingHopServer = nil
    isTeleporting = false

    UpdateStatus(
        "⚠️",
        Color3.fromRGB(255, 150, 0)
    )

    -- Fallback đúng 1 lần, không queue
    task.delay(0.5, function()
        if isTeleporting then
            return
        end

        local target = GetRandomAvailableServer()

        if not target then
            return
        end

        print(
            "[ServerHop] Fallback:",
            target.id,
            target.count .. "/" .. target.max
        )

        TeleportToServer(target.id)
    end)
end)


local function GetRandomAvailableServer()
    local allServers = GetServerList()

    if #allServers == 0 then
        warn("[ServerHop] No server list")
        return nil
    end

    local candidates = {}

    for _, server in ipairs(allServers) do
        if server.id
            and server.id ~= game.JobId
            and not IsBlacklisted(server.id)
            and server.count < server.max
        then
            table.insert(candidates, server)
        end
    end

    if #candidates == 0 then
        return nil
    end

    -- Không queue, chỉ lấy 1 server
    return candidates[math.random(1, #candidates)]
end


local function HopServer()
    if isTeleporting then
        return false
    end

    UpdateStatus(
        "🔍 Finding server...",
        Color3.fromRGB(255, 200, 0)
    )

    local target = GetRandomAvailableServer()

    if not target then
        UpdateStatus(
            "❌ No available server",
            Color3.fromRGB(255, 80, 80)
        )

        return false
    end

    -- Blacklist server hiện tại trước khi rời
    if game.JobId and game.JobId ~= "" then
        AddToBlacklist(game.JobId)
    end

    print(
        "[ServerHop] Target:",
        target.id,
        target.count .. "/" .. target.max
    )

    return TeleportToServer(target.id)
end

local function CreateUI()
    if plr.PlayerGui:FindFirstChild("FruitFarmUI") then return end
    
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "FruitFarmUI"
    screenGui.ResetOnSpawn = false
    screenGui.Parent = plr:WaitForChild("PlayerGui")
    
    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 280, 0, 140)
    mainFrame.Position = UDim2.new(0.5, -140, 0.5, -70)
    mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
    mainFrame.BackgroundTransparency = 0.1
    mainFrame.BorderSizePixel = 2
    mainFrame.BorderColor3 = Color3.fromRGB(255, 50, 50)
    mainFrame.Active = true
    mainFrame.Draggable = true
    mainFrame.Parent = screenGui
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 35)
    title.Position = UDim2.new(0, 0, 0, 0)
    title.Text = "🍎 Fruit Farm | Foxname ss2"
    title.TextColor3 = Color3.fromRGB(255, 50, 50)
    title.BackgroundTransparency = 1
    title.Font = Enum.Font.GothamBold
    title.TextSize = 22
    title.Parent = mainFrame
    
    local sub = Instance.new("TextLabel")
    sub.Size = UDim2.new(1, 0, 0, 20)
    sub.Position = UDim2.new(0, 0, 0, 35)
    sub.Text = "by Malority"
    sub.TextColor3 = Color3.fromRGB(200, 150, 150)
    sub.BackgroundTransparency = 1
    sub.Font = Enum.Font.GothamMedium
    sub.TextSize = 13
    sub.Parent = mainFrame
    
    statusLabel = Instance.new("TextLabel")
    statusLabel.Size = UDim2.new(1, 0, 0, 30)
    statusLabel.Position = UDim2.new(0, 0, 0, 58)
    statusLabel.Text = "🔄 Initializing..."
    statusLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
    statusLabel.BackgroundTransparency = 1
    statusLabel.Font = Enum.Font.GothamMedium
    statusLabel.TextSize = 14
    statusLabel.Parent = mainFrame
    
    local copyBtn = Instance.new("TextButton")
    copyBtn.Size = UDim2.new(0.8, 0, 0, 30)
    copyBtn.Position = UDim2.new(0.1, 0, 0, 95)
    copyBtn.Text = "📋 Join Discord"
    copyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    copyBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
    copyBtn.Font = Enum.Font.GothamBold
    copyBtn.TextSize = 14
    copyBtn.Parent = mainFrame
    
    copyBtn.MouseButton1Click:Connect(function()
        setclipboard("discord.gg/mSrMzVuc3h")
        copyBtn.Text = "Copied!"
        task.wait(2.5)
        copyBtn.Text = "📋 Join Discord"
    end)
    
    return screenGui
end

local function UpdateStatus(text, color)
    if statusLabel then
        statusLabel.Text = text
        if color then
            statusLabel.TextColor3 = color
        end
    end
end

-- ===== MAIN =====
LoadBlacklist()
SelectTeam()
CreateUI()
UpdateStatus("✅ Ready", Color3.fromRGB(0, 255, 0))

-- Scan
UpdateStatus("🔍 Scanning for fruits...", Color3.fromRGB(255, 255, 0))
task.wait(2)
local found = ScanFruits()

if #found > 0 then
    UpdateStatus("📦 Waiting for fruit...", Color3.fromRGB(0, 200, 255))
    task.wait(1)
    local stored = storeAllFruits()
    if #stored == 0 then
        task.wait(1)
        stored = storeAllFruits()
    end
    if #stored > 0 then
        for _, fruitName in ipairs(stored) do
            if not table.find(fruitLog, fruitName) then
                table.insert(fruitLog, fruitName)
                collectedCount = collectedCount + 1
                SendWebhook(fruitName)
            end
        end
        UpdateStatus("✅ Stored " .. #stored .. " fruit(s)", Color3.fromRGB(0, 255, 0))
    else
        UpdateStatus("❌ Store failed", Color3.fromRGB(255, 100, 100))
    end
else
    UpdateStatus("❌ No Fruit", Color3.fromRGB(255, 100, 100))
end

task.wait(2.5)
UpdateStatus("🔄 Hopping...", Color3.fromRGB(255, 200, 0))
HopServer()
